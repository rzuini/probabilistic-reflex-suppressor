# Computer vision skill

Treat detections as uncertain observations. Preserve confidence and bounding boxes, document coordinate conventions, and make image preprocessing explicit. Use synthetic arrays for deterministic geometry tests and representative consented data for calibration.

Never infer identity or intent from a detection. A reflection decision from a single image is probabilistic; expose the heuristic evidence and limitations to callers.
