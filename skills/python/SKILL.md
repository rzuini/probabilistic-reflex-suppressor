# Python skill

Use small typed modules, dataclasses for stable result objects, and dependency injection for external services or model backends. Keep filesystem and CLI concerns at the edge of the application. Run Ruff and mypy before handoff.

Prefer explicit exceptions with actionable messages. Avoid hidden global model state unless startup cost is measured and documented.
